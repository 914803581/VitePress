# markdown 语言的一点理解

- mark down 字面意思是“标记📌向下👇”，延伸开来就是简化标记📌，主要是针对 HTML 而言的，简化 HTML 复杂的标记📌符合🔣语法，也能实现特定的样式输出。

- markdown 语言实际上是内置了一些样式，这样我们就不用再写样式了，但局限性也是明显了，只能使用优先的样式，不能定制化样式，这是其轻量级的代价。简化的使用方式，但得付出“刻板”的代价。